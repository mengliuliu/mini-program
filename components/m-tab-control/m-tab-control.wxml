<!--pages/home/childCmps/m-tab-control/m-tab-control.wxml-->
<view class="tab-control">
  <block wx:for="{{titles}}" wx:key="*this">
    <view class='tab-item {{currentIndex === index ? "active" : ""}}'
          bind:tap="handleItemClick"
          data-index="{{index}}">
      <text>{{ item }}</text>
    </view>
  </block>
</view>
